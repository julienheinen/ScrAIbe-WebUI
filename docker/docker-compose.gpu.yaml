services:
  scraibe-webui:
    extends: 
      file: docker-compose.yaml
      service: scraibe-webui
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]